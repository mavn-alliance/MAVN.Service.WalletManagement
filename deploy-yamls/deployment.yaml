apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    description: Lykke.Service.WalletManagement
  labels:
    app: walletmanagement
    environment: Development
    tier: Service
  name: walletmanagement
  namespace: services
spec:
  revisionHistoryLimit: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: walletmanagement
  template:
    metadata:
      labels:
        app: walletmanagement
      name: walletmanagement
    spec:
 #     nodeSelector:
 #       type: 
 #     tolerations:
 #     - effect: NoSchedule
 #       operator: Exists
      imagePullSecrets:
      - name: walletmanagement-docker
      containers:
      - name: walletmanagement
        image: lykkebusiness/service-walletmanagement:dev
        imagePullPolicy: Always
        lifecycle:
          postStart:
            exec:
             command:
             - /bin/sh
             - -c 
             - "/bin/echo 'options single-request-reopen' >> /etc/resolv.conf"
        ports:
        - containerPort: 5000
        readinessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 20
        resources:
          requests:
            memory: 128Mi 
            cpu: 40m 
          limits:
            memory: 1024Mi 
            cpu: 1000m 
        env:
        - name: BuildNumber
          value: '$version'
        - name: SettingsUrl
          value: '$ConnectionString'
        - name: ENV_INFO
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: APPINSIGHTS_INSTRUMENTATIONKEY
          value: 
        - name: MyMonitoringUrl
          value: http://walletmanagement.services.svc.cluster.local/api/isalive
        - name: CPApiKey
          value: wallet-management-key
