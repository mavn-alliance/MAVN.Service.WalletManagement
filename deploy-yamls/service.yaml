apiVersion: v1
kind: Service
metadata:
  labels:
    app: walletmanagement
    environment: Development
  name: walletmanagement
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: walletmanagement
  sessionAffinity: None
  type: ClusterIP
